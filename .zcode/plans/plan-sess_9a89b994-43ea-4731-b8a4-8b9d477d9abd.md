1. 扩展 `crates/ipc/src/protocol.rs` 的 `SetConfigParams`，加入 `hide_on_focus_lost`、`hide_on_execute`、`launch_on_startup` 三个可选布尔字段，保持缺省字段不变并兼容现有热键请求。
2. 修改 `crates/host/src/ipc_server.rs` 的 `host.set_config`：逐字段比较并更新 `HostConfig`，一次性保存 `config.toml`；保留热键变化后的主线程重注册逻辑。这样三个设置以及热键都由 Host 统一持久化。
3. 扩展 UI `HostIpcClient`：增加 Host 配置 DTO、`GetConfigAsync` 和一次性 `SetConfigAsync`，让 JSON 属性名与 Rust serde 字段一致；保留现有查询/调用链路。
4. 调整 `MainWindow` 的启动和重连流程：连接成功后先调用 `host.get_config`，以 Host 返回值刷新 `LocalState`、运行时 `_hideOnDeactivate`、设置控件和热键显示；同步开机启动时按 Host 值维护注册表，并只把注册表作为执行落地，不再作为配置权威。删除启动/重连阶段把 `LocalState.Ui.Hotkey` 推回 Host 的逻辑，避免旧缓存覆盖 `config.toml`。
5. 调整三个开关及热键的用户操作：更新本地 UI 缓存仅用于界面/离线过渡，同时调用 `SetConfigAsync` 回写 Host；开机启动先执行注册表变更，再回写 Host，失败记录日志但不伪造成功状态。保留 `_syncing` 防止 Host 同步触发保存副作用。
6. 增加 Rust 协议/配置序列化覆盖测试（默认值、部分字段、三开关 set 参数），执行 `cargo fmt` 与 `cargo test --workspace`；再尝试 `dotnet build ui/Spark.UI/Spark.UI.csproj -c Debug`。如有编译或审查问题，修正后重复验证。
7. 按仓库规约调用 Code Auditor 子智能体审查修改，直到返回 `[STATUS: PASSED]`，再交付变更摘要和验证结果。