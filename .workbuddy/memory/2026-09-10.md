# 2026-09-10

## 插件未提交改动盘点（只读审计，未改代码）

盘点 `master` 上未提交内容：38 个已跟踪文件改动（+1873/-219），4 类新增未跟踪
（`crates/host/src/net_fetch.rs`、`crates/host/src/plugin_fs.rs`、`scripts/e2e/`、
`.zcode/plans/`）。

**在飞的工作流（均已完成自测、待提交）**
- `spark.net.fetch`：host 侧 WinHTTP 代理（`net_fetch.rs`，10 个单测），锁外执行；
  preload 去掉本地快拦改直连 host 鉴权；UI 超时 `net => 30s`。
- `spark.fs.read/write` + 授权目录范围（`plugin_fs.rs`，5 单测）：`PluginState.fs_scopes`
  持久化，目录 canonicalize，越界 `PERMISSION_SCOPE`，上限 10MB `FILE_TOO_LARGE`。
- `clipboard.readImage`（`clipboard.rs` +465 行，6 单测）：DIB→RGBA→WIC PNG→base64。
- `regex` / `root` 触发方式（`regex-lite`，加载期编译校验 + 上限 512 字节；热路径
  `regex_cache` 键控缓存）→ 候选新增 `plugin_command`/`plugin_input` 触发上下文。
- `shell.openExternal`（锁外 ShellExecute + 控制字符净化）。
- UI：fs 授权范围编辑对话框、`.spark-plugin` 本地 zip 安装、多权限全量覆盖 grant。
- CI：签名步骤输出 `signing-log.txt` 并上传 artifact。

**质量门禁实测**：`cargo test --workspace` 全绿（184 passed / 1 ignored），
`cargo fmt --all --check` 干净。

**结论：插件侧仍未完成项**（详见对话）
1. `clipboard.read/write`（ClipboardItem 复杂格式）仍是 UNAVAILABLE 桩。
2. 裸 `fetch()` 出站未收口（无 `WebResourceRequested` 过滤）→ `net` 权限门可绕过。
3. 插件多开未做（`PluginWindowHost.OpenOrFocus` 恒定单窗）。
4. 插件打包流水线未做 → CI 签名步骤实为 no-op；正式 `spark-official-v1` 密钥仍是开发密钥。
5. 市场 Phase 5 余项：本地示例仓库 + localhost 联调、非标准 registry.json 容错用例。
6. 安全整改 V5（host 更新链防降级）/ V6（吊销表条目）/ V7（插件目录写隔离）/ V9（权限最小化）未落地。
7. 三期：自动检查更新、星标/下载量统计、native `window.create`；四期：WASM、依赖声明、仓库推荐。
8. 文档漂移：`docs/FEATURES.md` §4.8 仍是旧的 `.lap`/列表项/全局贡献愿景，与现行 page-only 模型不一致。

---

## 第二轮：按用户要求「尽量全做」+ 市场 tags 分组（代码已改，未提交）

用户选择范围 = 尽量全做；分组维度 = registry.json 加 tags 多标签。本轮改动：

1. **裸 fetch 出站收口**（P0）：`PluginWindow.InstallOutboundGuard/OnWebResourceRequested/BlockOutbound`
   —— `AddWebResourceRequestedFilter("*", All)` + 放行虚拟主机与非 http(s) scheme，其余 403。
   preload 第二层封死 `WebSocket`/`WebTransport`/`RTCPeerConnection`（不走 HTTP 请求栈，引擎拦不到）。
2. **市场 tags 分类分组**：`RegistryPluginDto.Tags` + `RegistryService.NormalizeTags`
   （≤12 字符/≤4 个/去控制字符/忽略大小写去重）+ 市场「分类」chip 筛选栏 + 按首个标签的组头行。
   ListView 保持扁平 ItemsSource，用 `MarketRowTemplateSelector` 派发组头/卡片（**关键**：
   自定义 ItemContainerStyle 的 ControlTemplate 必须转发 `ContentTemplateSelector`，否则整列空白）。
3. **自动检查更新 + 批量更新**：`RefreshMarketUpdateNotice` / `OnUpdateAllMarket`（汇总确认逐项点名风险，
   串行安装）+ 筛选栏「可更新」。
4. **插件多开**：`PluginWindow.multi_instance`（+ camelCase 归一化）→ `PluginOpenInfoDto.Window.MultiInstance`
   → `PluginWindowHost` 改为 `Dictionary<string, List<PluginWindow>>`，`HasOtherOpen(id, except)`
   保证 native exe **最后一个窗口关闭才关停**。
5. **clipboard.read/write 多格式**：host 侧 `ClipboardRequest::{ReadItem,WriteItem}` +
   `read_all`/`write_item`；PNG→WIC 解码→BGRA→CF_DIB 写入（`bgra_to_dib`），文本+图片可同时写；
   锁外执行（`PluginApiOutcome::Clipboard`）；`open_clipboard()` 有界重试（10×10ms，剪贴板独占语义）。
6. **文档**：规范 §4.3/§6/§8/§15 + 错误码表、市场与仓库 §3.3/§9.5、WebView 插件开发 §4.2、
   FEATURES.md §3/§4.8 全部对齐；PLUGIN_MARKETPLACE_TASKS 补 §5.5。
7. **e2e**：新增 `scripts/e2e/clipboard-item.ps1`（多格式往返）与 `scripts/e2e/market-local.ps1`
   （本地仓库 fixture + 联通性，已实测 PASSED）。

**Code Auditor 闭环**：无 🔴，7 条 🟡 → 已修 4 条（`CloseAll` 枚举期改集合、出站回调缺 try/catch、
`_blockedHosts` 无界、`ClassifyError` 漏 `FILE_TOO_LARGE`/`UNSUPPORTED_FORMAT`）、
1 条改设计（serde alias 同现硬失败 → 改为 `load()` 前 `normalize_window_keys` 归一化）、
1 条落文档（批量更新聚合确认等价性写入整改清单 V8）。

**门禁**：`cargo fmt --all --check` 干净；`cargo test --workspace` 190 passed / 1 ignored；
`dotnet build` UI 0 error（被占用时只在拷贝阶段失败）。剪贴板 e2e **未能在本环境验证**
（无头会话打不开剪贴板，仓库原有 clipboard-image.ps1 同样失败），需用户在交互桌面跑。

