<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Spark.UI.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12">
            <Button Content="← 返回" Click="Back_Click"/>
            <TextBlock Text="设置" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Spacing="12" Margin="0,12,0,0">
            <ToggleSwitch Header="开机启动" IsOn="True"/>
            <ToggleSwitch Header="失焦时隐藏" IsOn="True"/>
            <TextBlock TextWrapping="Wrap" Opacity="0.7"
                       Text="默认热键 Alt+Space，可改为 Ctrl+Space。系统托盘由 spark-host 负责。"/>
        </StackPanel>
    </Grid>
</UserControl>
